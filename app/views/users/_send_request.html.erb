<%= form_for(current_user.sent_friend_requests.build,
             remote: true
             ) do |f| %>
  <div><%= hidden_field_tag :recipient_id, @user.id %></div>
  <%= f.submit "Send Friend Request", class: "button" %>
<% end %>
