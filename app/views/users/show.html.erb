<h1><%= @user.name %>'s Profile</h1>

<div>
  <div>
    <%= render 'shared/stats' %>
  </section>
  <div>
    <%= render 'friend_form' if user_signed_in? %>
  </div>
</div>

<div class="pending_requests">
  <%= render 'pending_requests_form' if current_user == @user%>
</div>

<div class=links>
  <%= link_to "All users", users_path %> |
  <%= link_to "Home", root_path %>
</div>
