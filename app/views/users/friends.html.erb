<h1><%= @user.name %>'s friends</h1>

<div class="user_info">
    <%= render 'shared/info' %>
</div>

<div class="friends">
  <% if @users.any? %>
    <ul class="users friends">
      <%= render @users %>
    </ul>
    <%= will_paginate %>
  <% end %>
</div>
